import React from "react";
import classes from "./Navbar.module.css";

const Navbar = () => {
  return (
    <header>
      <nav className={classes.container}>navbar</nav>
    </header>
  );
};

export default Navbar;
